/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['example.com'], // Reemplace con los dominios de sus imágenes externas
  },
}

module.exports = nextConfig